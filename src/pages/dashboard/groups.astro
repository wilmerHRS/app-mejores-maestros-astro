---
import DashboardLayout from '@/layouts/DashboardLayout.astro';
import { GroupsTab } from '@/_pages/dashboard';
import { getDashboardData } from '@/_pages/dashboard/index.server';

const { profile, congregation, userEmail } = await getDashboardData(Astro.cookies, Astro.redirect);
---
<DashboardLayout activeTab="groups" profile={profile} congregation={congregation} userEmail={userEmail}>
  <GroupsTab client:load congregationId={profile.congregationId} />
</DashboardLayout>
