---
import DashboardLayout from '@/layouts/DashboardLayout.astro';
import { SettingsTab } from '@/_pages/dashboard';
import { getDashboardData } from '@/_pages/dashboard/index.server';

const { profile, congregation, userEmail } = await getDashboardData(Astro.cookies, Astro.redirect);
---
<DashboardLayout activeTab="settings" profile={profile} congregation={congregation} userEmail={userEmail}>
  <SettingsTab client:load congregation={congregation} />
</DashboardLayout>
